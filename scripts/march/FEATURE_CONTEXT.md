# Feature Context — copilot-cli-python

> Fix Shift+Tab mode toggle so that cycling INTERACTIVE → PLAN → AUTOPILOT triggers a conversation-view notification, prefixes plan-mode messages with [[PLAN]] before sending to the AI, and injects the canonical plan-mode / autopilot system-prompt sections — matching the original Copilot CLI behaviour exactly.

> **Auto-generated sections below — custom sections are preserved.**
> Add custom notes in the 'Custom Context' section at the bottom.

## Story Groups

### Textual
- **US-001**: Mode change notification via Textual message (priority 1)
- **US-002**: Message prefix injection based on mode (priority 2)

## Schema Extensions

_No schema field references detected in acceptance criteria._

## Cross-Module Touch Points

_No cross-module touch points detected._

## Known Constraints

_No constraints defined in aes.json. Add a `constraints` list if needed._
